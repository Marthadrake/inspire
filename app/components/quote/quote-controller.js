import QuoteService from "./quote-service.js";

let _qs = new QuoteService()

/** TODO define a drawQuote function
 * 1. get the quote info from the service
 * 2. target an element on the DOM that will hold the template of your quote
 * 3. assign the innerHTML of that element to the retrieved quote info
 *  */

export default class QuoteController {
  // TODO add a constructor method and within the code block of the constructor don't forget to add your drawQuote function as a subscriber
}
